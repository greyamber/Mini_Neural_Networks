本次更新：
1.添加多个全局变量控制
2.求和函数改为动态，不需要再传入x1shape
3.递归改循环:bp,fp等必须递归的算法改为循环实现，clear_all等函数使用全局列表控制。
4.添加一些+-*/的功能，实现左右都可以运算。
5.添加log函数
6.移除以前的用例，添加新的用例

下次更新：
1.完成对shape的检查（由于时间这次没实现）
2.实现卷积，pooling，mean等常用操作。
3.添加一些优化器的实现逻辑（不改最终的gradient_decent(lr)操作,在梯度下降之强修改梯度）
